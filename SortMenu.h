#ifndef SORTMENU_H
#define SORTMENU_H

#include <SFML/Graphics.hpp>
using namespace std;

// Función para manejar el menú de ordenamiento
void sortMenu(sf::RenderWindow &window, sf::Font &font);

#endif // SORTMENU_H
